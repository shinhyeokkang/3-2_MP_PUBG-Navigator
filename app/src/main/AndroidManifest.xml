<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.map">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Category">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".weapon.Grid" />
        <activity android:name=".weapon.Weapon"/>
        <activity android:name=".Map.Citydata" />
        <activity android:name=".Map.Map" />
        <activity android:name=".Detail"/>
        <activity android:name=".weapon.Ammunition"/>
        <activity android:name=".weapon.Throwables"/>
        <activity android:name=".weapon.Consumable"/>
        <activity android:name=".weapon.Melee"/>
        <activity android:name=".weapon.Equipment"/>

    </application>

</manifest>